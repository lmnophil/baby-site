# Baby Site

Baby Site is a static, personal baby-reference website. It is intended to keep
lightweight, accessible reference information in one place using plain HTML and
CSS.

The project currently has no dependencies, build system, analytics, or
deployment configuration. Open `index.html` directly in a web browser to view
the site.

